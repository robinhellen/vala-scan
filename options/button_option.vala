using Sane;

namespace Scan
{
    public class ButtonOption : Option
    {
        internal ButtonOption(OptionDescriptor o, ScannerSession s, Int n)
        {
            base(o, s, n);
        }
    }
}
